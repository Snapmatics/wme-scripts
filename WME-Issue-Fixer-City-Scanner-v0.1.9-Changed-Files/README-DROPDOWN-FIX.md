# Dropdown repair

This build replaces the broken duplicate/native dropdown rendering with a single themed custom dropdown layer.

Reload the extension and then reload the dashboard tab after replacing the files.
